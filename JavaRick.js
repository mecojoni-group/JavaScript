  // script javascript esterno
  let message = 'I\'m a EXTERNAL JavaRick';
  const data_nascita = '02/01/1995';
  
  alert( message );
  alert( "la mia data di nascita e\' il " + data_nascita );
  let age = prompt( "E tu quanti anni hai?", 100 );
  let conferma = confirm( "Hai davvero " + age + " anni?" );
  alert ( "Vecchioo !" ); 
  /*commento
  
  AND ( & )
  OR ( | )
  XOR ( ^ )
  NOT ( ~ )
  LEFT SHIFT ( << )
  RIGHT SHIFT ( >> )
  ZERO-FILL RIGHT SHIFT ( >>> )
  
  
  // three operations in one line - for cicle
  for (a = 1, b = 3, c = a * b; a < 10; a++) {
  	...
  }
  
  multilinea*/ 